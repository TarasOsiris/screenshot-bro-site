User-agent: *
Allow: /
Disallow: /build/
# Cloudflare rewrites mailto: links to /cdn-cgi/l/email-protection#<hash>. Crawlers
# drop the fragment and the bare URL 404s, so keep them out of the index entirely.
Disallow: /cdn-cgi/
# React Router serves every page's loader data as a parallel <path>.data URL.
# They duplicate page content as opaque payloads, so keep crawl budget off them.
Disallow: /*.data$
Disallow: /*.data?

# Answer engines and AI crawlers. Google-Extended and Applebot-Extended are
# opt-outs that some sites set by default, so state the intent explicitly:
# this site wants to be quoted. Per RFC 9309 a crawler matching one of these
# groups ignores the * group above, so the disallows are repeated here.
User-agent: GPTBot
User-agent: OAI-SearchBot
User-agent: ChatGPT-User
User-agent: ClaudeBot
User-agent: Claude-User
User-agent: Claude-SearchBot
User-agent: PerplexityBot
User-agent: Perplexity-User
User-agent: Google-Extended
User-agent: Applebot
User-agent: Applebot-Extended
User-agent: CCBot
Allow: /
Disallow: /build/
Disallow: /cdn-cgi/
Disallow: /*.data$
Disallow: /*.data?

Sitemap: https://screenshotbro.app/sitemap.xml
